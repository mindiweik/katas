// @ts-check

/** @type {import("prettier").Config} */
const config = {
  bracketSpacing: true,
  embeddedLanguageFormatting: "auto",
  parser: "typescript",
  printWidth: 100,
  semi: false,
  singleQuote: true,
  tabWidth: 2,
  trailingComma: "all",
  useTabs: false,
}

export default config
